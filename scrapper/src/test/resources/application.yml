app:
  scheduler:
    enable: false
    interval: 10s
    force-check-delay: 10s
  github-base-url: http://localhost:8081
  stackoverflow-base-url: http://localhost:8081
  bot-base-url: http://localhost:8090

spring:
  jackson:
    property-naming-strategy: SNAKE_CASE
  application:
    name: scrapper
  liquibase:
    enabled: false

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml

wire-mock:
  port: 8081
